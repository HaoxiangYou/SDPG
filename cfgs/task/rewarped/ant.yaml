backend: rewarped

name: ant

config:
  
  suite: dflex
  num_envs: ${...num_envs}

  randomize: True
  no_grad: True
  render: True
  
  # rewarped backend specific terms
  no_env_offset: False
  render_mode: "usd"
  # render_output_dir: null  # If null, uses Hydra's output directory. Otherwise, specify a path for USD files.

